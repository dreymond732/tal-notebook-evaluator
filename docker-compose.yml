services:
  web-corrector:
    build: .
    container_name: correcteurtilt
    restart: unless-stopped
    ports:
      - "2026:5001"
    volumes:
      # Ajout de ce volume pour la persistance des données
      - ./soumissions:/app/soumissions
      # - ./app:/app/  # pour dev c'est pratique
    networks:
      - proxnet

networks:
  proxnet:
    external: true